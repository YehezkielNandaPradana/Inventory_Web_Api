-- Optional MySQL initialization script
-- This runs when the MySQL container is initialized for the first time.
-- The database, user, and permissions are already created via MYSQL_* environment variables
-- in docker-compose.yml (MYSQL_DATABASE, MYSQL_USER, MYSQL_PASSWORD).

-- Example: set timezone for the session (optional)
-- SET time_zone = '+00:00';
